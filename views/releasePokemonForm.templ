package views

import ("fmt")

templ ReleasePokemonForm(id uint) {
    <form class="poke-release" method="POST" action="">
        <input hidden value={ fmt.Sprint(id) } type="text" />
        @Button(false, false, "Release")
    </form>
}