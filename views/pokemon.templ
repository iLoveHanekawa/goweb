package views

templ Pokemon(id string, name string) {
    <div>
        <h3 className="poke-name">{name}</h3>
        <h4 className="poke-type">{id}</h4>
        @UpdatePokemonForm(id)
        @ReleasePokemonForm(id)
    </div>
}